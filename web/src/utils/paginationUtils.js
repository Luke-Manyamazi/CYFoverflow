export const calculateTotalPages = (totalItems, itemsPerPage = 10) => {
	return 0;
};

export const getPageNumbers = (currentPage, totalPages, maxVisible = 5) => {
	return [];
};

export const getPaginationRange = (currentPage, itemsPerPage = 10) => {
	return { start: 0, end: 0 };
};

export const validatePageNumber = (page, totalPages) => {
	return 1;
};

